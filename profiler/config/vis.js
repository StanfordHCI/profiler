dp.config = {
  vis: {
    // Max rows/cols in the grid layout.
    max_rows: 4,
    max_cols: 3,
    // Dimensions of individual cells in grid layout.
    cell_height: 100,
    cell_width: 230,
    // Margins between cells in grid layout.
    cell_vertical_margin: 12,
    cell_horizontal_margin: 8,
    // Height of the quality bar above visualizations.
    quality_bar_height: 18,
    // Offset of title within quality bar.
    title_x: 4,
    title_y: 14,
    // Height/width of bars in charts
    bar_height: 16,
    bar_spacing: 2,
    // Offset of labels within bars
    bar_label_x: "4px",
    bar_label_y: "1.15em",
    // Height of the gutter beneath the chart used for labeling
    xaxis_gutter_height: 16,
    // Offset of labels within the gutter
    xaxis_vertical_padding: 14,
    // Width of the gutter to the right of the chart used for labeling
    yaxis_gutter_width: 34,
    // Offset of labels within the gutter
    yaxis_horizontal_padding: 4,
    // Offset to account for varying length labels.
    axis_character_offset: 8,
    // Offset to center map.
    map_padding_x: 10,
    map_padding_y: 0
  }
};
